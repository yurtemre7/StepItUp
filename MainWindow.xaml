<Window x:Class="StepItUp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="StepItUp Overlay" Height="150" Width="250"
        Icon="StepItUp.ico"
        AllowsTransparency="True" WindowStyle="None" Topmost="True" Background="#88000000"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="BorderBrush" Value="#FF00569C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="Red"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Foreground" Value="DarkRed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Close button path for the X shape -->
        <PathGeometry x:Key="CloseButtonPath" Figures="M0,0 L1,1 M0,1 L1,0" />

        <Style x:Key="TransparentTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="BorderBrush" Value="#66FFFFFF"/>
            <!-- Semi-transparent white -->
            <Setter Property="BorderThickness" Value="1"/>
            <!-- Adjust thickness as needed -->
            <Setter Property="Padding" Value="3"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <!-- Ensures the cursor is white -->
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#AAFFFFFF"/>
                    <!-- Slightly less transparent when hovered -->
                </Trigger>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="#FFFFFFFF"/>
                    <!-- Fully opaque when focused -->
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button x:Name="closeButton" Content="&#xE106;" HorizontalAlignment="Right" VerticalAlignment="Top"
            Width="30" Height="30" Margin="0,0,0,0" Click="CloseButton_Click"
            Style="{StaticResource CloseButtonStyle}" FontFamily="Segoe MDL2 Assets"/>

        <Image Source="StepItUp.ico" Width="30" Height="30" Margin="2" HorizontalAlignment="Left" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label x:Name="titleLabel" Content="StepItUp Overlay" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="20" Foreground="White"/>
        </StackPanel>

        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button x:Name="minusButton" Content="-" Click="Minus_Click" Style="{StaticResource RoundedButton}" Width="32" Height="32"/>
            <Label x:Name="numberLabel" Content="0" Width="40" Height="40" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Foreground="White"/>
            <Button x:Name="plusButton" Content="+" Click="Plus_Click" Style="{StaticResource RoundedButton}" Width="32" Height="32"/>
            <Button x:Name="resetButton" Content="Reset" Click="Reset_Click" Style="{StaticResource RoundedButton}" Width="54" Height="32"  Margin="10"/>
        </StackPanel>

        <!-- Topic Label and Text Field -->
        <StackPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0">
            <TextBox x:Name="topicTextBox" Width="200" FontSize="20" Style="{StaticResource TransparentTextBox}" TextChanged="CounterText_TextChanged"/>
        </StackPanel>
    </Grid>
</Window>
